package streams.waterpouring

import org.scalatest.{FlatSpec, Matchers}

class SolverTest extends FlatSpec with Matchers {
//  val solver = Solver(capacities = Set(3, 5), target = 4)
//
//  "a Solver" should "identify valid end states" in {
//    solver.accepts(State(Set(Glass(5, 5), Glass(3, 3)))) shouldBe false
//    solver.accepts(State(Set(Glass(5, 4), Glass(3, 3)))) shouldBe true
//  }
//
//  it should "find a solution for 2 glasses" in {
//    solver.solve match {
//      case Some(Path(state, moves)) =>
//        state shouldBe State(Set(Glass(5, 4), Glass(3, 3)))
//        moves should have size 6
//    }
//  }
//
//  it should "find a solution for 3 glasses" in {
//    val solver = Solver(capacities = Set(3, 7, 13), target = 5)
//
//    solver.solve match {
//      case Some(Path(state, moves)) =>
//        state shouldBe State(Set(Glass(3, 3), Glass(7, 5), Glass(13, 13)))
//        moves should have size 7
//    }
//  }
//
//  it should "find a solution for 4 glasses" in {
//    val solver = Solver(capacities = Set(2, 7, 19, 47), target = 20)
//
//    solver.solve match {
//      case Some(Path(state, moves)) =>
//        state shouldBe State(Set(Glass(2, 2), Glass(7, 2), Glass(19, 2), Glass(47, 20)))
//        moves should have size 9
//    }
//  }
//
}
